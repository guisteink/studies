read x
read y
read z

identifyTriangle() {
    if [ "$x" -eq "$y" ] && [ "$y" -eq "$z" ]; then
        echo "EQUILATERAL"
    elif [ "$x" -ne "$y" ] && [ "$y" -ne "$z" ]; then
        echo "SCALENE"
    else 
        echo "ISOSCELES"
    fi
}

identifyTriangle
